// Generated by CoffeeScript 1.6.2
var helper, print, pull;

print = require('sys').print;

helper = require('../lib/helper');

pull = function(project) {
  var cmd;

  cmd = helper.exec('pull', project);
  return cmd.on('close', function(code) {
    if (code !== 0) {
      return print('操作失败，请检查原因\n ');
    }
    print("操作成功！！\n");
    return process.exit();
  });
};

module.exports = pull;

if (process.argv[1].endsWith("pull") || process.argv[1].endsWith("pull.js")) {
  helper.getName(pull);
}
